import logo from "./assets/logo.png";

const App = () => {
    return (
        <div>
          <h1>Hello, React + TypeScript + Rollup!</h1>
          <img src={logo} alt="Logo" />
        </div>
    );
};

export default App;